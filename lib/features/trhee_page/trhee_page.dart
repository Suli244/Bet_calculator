import 'package:flutter/material.dart';

class TrheePage extends StatelessWidget {
  const TrheePage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('TrheePage'),
      ),
      body: Column(
        children: [
          Container(),
        ],
      ),
    );
  }
}